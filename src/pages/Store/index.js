import React from 'react'
import Layout from '../../components/Layout';

const Store = ({children}) => {
    return (
        <Layout>
            {children}
        </Layout>
    )
}

export default Store;
