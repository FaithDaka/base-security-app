import React from 'react';
import Layout from '../../components/Layout';

const index = ({children}) => {
    return (
        <Layout>
            {children}
        </Layout>
    )
}

export default index
